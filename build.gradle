buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.2.3'
    }
}

allprojects {
    repositories {
        flatDir {
            dirs "$rootProject.projectDir/net-speech-api/target/"
        }
        mavenCentral()
    }
}
